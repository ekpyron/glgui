find_package (oglp REQUIRED)
find_package (glm REQUIRED)

set (GLGUI_INCLUDE_DIRS @CMAKE_INSTALL_PREFIX@/include ${OGLP_INCLUDE_DIRS} ${GLM_INCLUDE_DIRS})
set (GLGUI_FOUND TRUE)
find_library (GLGUI_LIBRARY glgui PATHS @CMAKE_INSTALL_PREFIX@/lib)
set (GLGUI_LIBRARIES ${GLGUI_LIBRARY} ${OGLP_LIBRARIES})
